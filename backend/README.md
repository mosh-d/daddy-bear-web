# Daddy Bear — backend

Reserved for Phase 2 (September, per `../EXECUTION_PLAN.md`): the NestJS + PostgreSQL API covering orders, tickets, payments and the admin dashboard, deployed to Render separately from `../frontend` on Vercel.

Empty on purpose — scaffolding this now would sit untouched for weeks. It gets built when Phase 2 starts, against current dependency versions at that time.
